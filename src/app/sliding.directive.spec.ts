import { SlidingDirective } from './sliding.directive';

describe('SlidingDirective', () => {
  it('should create an instance', () => {
    const directive = new SlidingDirective();
    expect(directive).toBeTruthy();
  });
});
